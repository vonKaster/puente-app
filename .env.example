# Variables de entorno para Docker Compose y desarrollo
# Copiar este archivo a .env y configurar con valores reales

# === AlphaVantage API ===
# Obtener API key gratuita en: https://www.alphavantage.co/support/#api-key
# Tier gratuito: 25 requests/día, 5 requests/minuto
ALPHAVANTAGE_API_KEY=your_api_key_here

# === Base de Datos (PostgreSQL) ===
DB_HOST=postgres
DB_PORT=5432
DB_NAME=puente
DB_USER=puente
DB_PASSWORD=puente123

# === Seguridad ===
# JWT Secret (mínimo 256 bits / 32 caracteres)
# Generar con: openssl rand -base64 32
JWT_SECRET=change-this-secret-in-production-minimum-256-bits-required
